# Dependencies
node_modules/

# Environment variables (Secrets)
.env

# System Files (Junk that Windows/Mac creates)
.DS_Store
Thumbs.db

# Logs (If npm crashes, it creates these)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories (Optional: if you don't want to share local settings)
.vscode/